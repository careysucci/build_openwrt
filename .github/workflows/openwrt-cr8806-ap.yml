#
#
#
# File: .github/workflows/openwrt-cr8806-ap.yml
# Description: Build ImmortalWrt (openwrt-25.12) AP firmware for Xiaomi CR8806 / Redmi AX3000
#
# Target:  qualcommax/ipq50xx (Qualcomm IPQ5018, aarch64, kernel 6.12)
# Source:  official immortalwrt/immortalwrt @ openwrt-25.12, with the CR880x
#          device support injected by diy/cr8806/imwrt-25.12 (DTS, base-files,
#          ipq-wifi BDF entry, ath11k board_id patches — ported from the
#          kmiit/Redmi_AX3000_immortalwrt adaptation, most of which is already
#          upstream in 25.12: QCN6122, RSSI fix, MPD, ge_phy, caldata helpers).
#          Full ath11k stack: 2.4G (IPQ5018) + 5G (QCN6122) both work.
# Profile: pure bridged AP — no proxy/NAS apps; first boot applies
#          bridge + dual-band WiFi (802.11r/k/v) via diy/cr8806/99-ap-bridge.
#
# Images:  bin/targets/qualcommax/ipq50xx/
#            *-squashfs-factory.ubi     → flash via U-boot TFTP
#            *-squashfs-sysupgrade.bin  → flash via LuCI/sysupgrade
#
# Independence: this workflow uses diy/cr8806/diy-ap.sh only; the shared
# diy-part1.sh / diy-part2.sh are untouched (LEDE/official unaffected).
#
# This is free software, licensed under the MIT License.
# See /LICENSE for more information.
#

name: OpenWrt CR8806 AP (ImmortalWrt) Builder

on:
  repository_dispatch:
  workflow_dispatch:
    inputs:
      ssh_enabled:
        description: 'ssh'
        type: boolean
        required: false
        default: false
      clear_actions_cache:
        description: 'clear actions cache'
        type: boolean
        required: false
        default: false

env:
  REPO_URL: https://github.com/immortalwrt/immortalwrt.git
  REPO_BRANCH: openwrt-25.12
  CONFIG_FILE: diy/cr8806/immortalwrt_ap.config
  DIY_AP_SH: diy/cr8806/diy-ap.sh
  ADAPT_SH: diy/cr8806/imwrt-25.12/apply-cr8806.sh
  UPLOAD_BIN_DIR: false
  UPLOAD_FIRMWARE: true
  UPLOAD_RELEASE: true
  AUTHORED_BY: Wy.House
  IMG_PREFIX: WyHouse
  TARGET_MATRIX: imwrt
  RELEASE_NAME: WyWrt-AP
  TZ: Asia/Shanghai

jobs:
  build:
    runs-on: ubuntu-22.04

    permissions:
      contents: write

    steps:
      - name: clear space cache
        if: ${{ github.event_name == 'workflow_dispatch' && inputs.clear_actions_cache }}
        uses: jlumbroso/free-disk-space@main
        with:
          tool-cache: true
          android: true
          dotnet: true
          haskell: true
          large-packages: true
          swap-storage: true

      - name: Checkout
        uses: actions/checkout@v4.2.2

      - name: Initialization environment
        env:
          DEBIAN_FRONTEND: noninteractive
        run: |
          sudo rm -rf /etc/apt/sources.list.d/* /usr/share/dotnet /usr/local/lib/android /opt/ghc /opt/hostedtoolcache/CodeQL
          sudo docker image prune --all --force
          sudo -E apt-get -qq update
          sudo -E apt-get -qq install build-essential clang flex bison g++ gawk \
          gcc-multilib g++-multilib gettext git libncurses5-dev libssl-dev \
          python3-setuptools rsync swig unzip zlib1g-dev file wget \
          llvm python3-pyelftools libpython3-dev aria2 jq qemu-utils ccache rename \
          libelf-dev device-tree-compiler libgmp3-dev libmpc-dev libfuse-dev dwarves zstd
          sudo -E apt-get -qq autoremove --purge
          sudo -E apt-get -qq clean
          sudo timedatectl set-timezone "$TZ"
          sudo mkdir -p /workdir
          sudo chown $USER:$GROUPS /workdir

      - name: Get current date
        id: date
        run: |
          echo "DATE1=$(date +'%Y.%m.%d-%H%M')" >> $GITHUB_ENV
          echo "DATE2=$(date +'%m/%d %Y')" >> $GITHUB_ENV
          echo "DATE3=$(date +'%m.%d')" >> $GITHUB_ENV
          echo "DATE4=$(date +'%Y.%m.%d')" >> $GITHUB_ENV

      - name: Clone source code
        working-directory: /workdir
        run: |
          df -hT $PWD
          git clone $REPO_URL -b $REPO_BRANCH ${{ env.TARGET_MATRIX }}
          ln -sf /workdir/${{ env.TARGET_MATRIX }} $GITHUB_WORKSPACE/${{ env.TARGET_MATRIX }}

      - name: Free up disk space
        env:
          DEBIAN_FRONTEND: noninteractive
        run: |
          sudo mkdir -p -m 777 /mnt/${{ env.TARGET_MATRIX }}/dl /mnt/${{ env.TARGET_MATRIX }}/bin /mnt/${{ env.TARGET_MATRIX }}/staging_dir
          ln -sf /mnt/${{ env.TARGET_MATRIX }}/dl ${{ env.TARGET_MATRIX }}/dl
          ln -sf /mnt/${{ env.TARGET_MATRIX }}/bin ${{ env.TARGET_MATRIX }}/bin
          ln -sf /mnt/${{ env.TARGET_MATRIX }}/staging_dir ${{ env.TARGET_MATRIX }}/staging_dir

      - name: Load custom configuration
        run: |
          # AP build: standalone diy script (banner/branding/first-boot AP
          # provisioning). No third-party feeds — plain ImmortalWrt feeds only.
          chmod +x ./$DIY_AP_SH && ./$DIY_AP_SH
          cd ${{ env.TARGET_MATRIX }}
          # Inject CR8806 device support into the official 25.12 tree
          # (must happen before defconfig so the device target exists).
          bash $GITHUB_WORKSPACE/$ADAPT_SH
          ./scripts/feeds update -a && ./scripts/feeds install -a
          cp -f $GITHUB_WORKSPACE/${{ env.CONFIG_FILE }} .config
          make defconfig

      - name: SSH Enabled Debug
        if: ${{ github.event_name == 'workflow_dispatch' && inputs.ssh_enabled }}
        uses: mxschmitt/action-tmate@v3
        timeout-minutes: 15
        with:
          limit-access-to-actor: true

      - name: Download package
        id: package
        run: |
          cd ${{ env.TARGET_MATRIX }}
          make download -j$(($(nproc)+1))
          find dl -size -1024c -exec ls -l {} \;
          find dl -size -1024c -exec rm -f {} \;
          make download -j$(($(nproc)+1))
          echo "[WORKFLOW] Package download completed"

      - name: Compile the firmware
        id: compile
        run: |
          cd ${{ env.TARGET_MATRIX }}
          echo -e "$(nproc) thread compile"
          make -j$(nproc) || make -j1 V=s
          echo "status=success" >> $GITHUB_OUTPUT
          grep '^CONFIG_TARGET.*DEVICE.*=y' .config | sed -r 's/.*DEVICE_(.*)=y/\1/' > DEVICE_NAME
          [ -s DEVICE_NAME ] && echo "DEVICE_NAME=_$(cat DEVICE_NAME)" >> $GITHUB_ENV
          echo "FILE_DATE=_$(date +"%Y%m%d%H%M")" >> $GITHUB_ENV

      - name: SSH Failed Debug
        if: ${{ failure() }}
        uses: mxschmitt/action-tmate@v3
        timeout-minutes: 15
        with:
          limit-access-to-actor: true

      - name: Check space usage
        if: (!cancelled())
        run: df -hT

      - name: Upload bin directory
        uses: actions/upload-artifact@main
        if: steps.compile.outputs.status == 'success' && env.UPLOAD_BIN_DIR == 'true'
        with:
          name: OpenWrt_bin${{ env.DEVICE_NAME }}${{ env.FILE_DATE }}
          path: ${{ env.TARGET_MATRIX }}/bin

      - name: Organize files
        id: organize
        if: env.UPLOAD_FIRMWARE == 'true' && !cancelled()
        run: |
          cd ${{ env.TARGET_MATRIX }}/bin/targets/*/*
          rm -rf packages
          # Remove build metadata files — keep only flashable images
          rm -f config.buildinfo feeds.buildinfo profiles.json version.buildinfo
          echo "FIRMWARE=$PWD" >> $GITHUB_ENV
          echo "status=success" >> $GITHUB_OUTPUT

      - name: Upload firmware directory
        uses: actions/upload-artifact@main
        if: steps.organize.outputs.status == 'success' && !cancelled()
        with:
          name: ${{ env.IMG_PREFIX }}_firmware${{ env.DEVICE_NAME }}${{ env.FILE_DATE }}
          path: ${{ env.FIRMWARE }}

      - name: Generate release tag
        id: tag
        if: env.UPLOAD_RELEASE == 'true' && !cancelled()
        run: |
          echo "release_tag=$(date +"%Y.%m.%d-%H%M")" >> $GITHUB_OUTPUT
          touch release.txt
          echo "OpenWrt CR8806 AP (ImmortalWrt openwrt-25.12) Build" >> release.txt
          echo "Target: ${{ env.RELEASE_NAME }} (qualcommax/ipq50xx, kernel 6.12)" >> release.txt
          echo "Mode: bridged AP — all ports bridged, DHCP/firewall off, mgmt http://172.16.3.19" >> release.txt
          echo "WiFi: 2.4G+5G SSID 'WyHouse' (key wyhouse8888), 802.11r/k/v roaming enabled" >> release.txt
          echo "Flash: *-squashfs-factory.ubi via U-boot TFTP | *-squashfs-sysupgrade.bin via LuCI" >> release.txt
          echo "Date: $(date)" >> release.txt
          echo "status=success" >> $GITHUB_OUTPUT

      - name: Upload firmware to release
        uses: softprops/action-gh-release@master
        if: steps.tag.outputs.status == 'success' && !cancelled()
        env:
          GITHUB_TOKEN: ${{ secrets.ACTIONS_TOKEN }}
        with:
          tag_name: ${{ steps.tag.outputs.release_tag }}
          body_path: release.txt
          files: ${{ env.FIRMWARE }}/*

      - name: Delete workflow runs
        uses: Mattraks/delete-workflow-runs@main
        with:
          retain_days: 0
          keep_minimum_runs: 2

      - name: Remove old Releases
        uses: dev-drprasad/delete-older-releases@master
        if: env.UPLOAD_RELEASE == 'true' && !cancelled()
        with:
          keep_latest: 8
          delete_tags: true
        env:
          GITHUB_TOKEN: ${{ secrets.ACTIONS_TOKEN }}
